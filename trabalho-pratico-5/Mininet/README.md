# Mininet

Execute a topologia pela raiz do repositorio:

```bash
sudo python3 topo.py
```

Ou pela entrada alternativa exigida na estrutura de entrega:

```bash
sudo python3 Mininet/topology.py
```

A topologia contem `r1`, `r2`, `r3`, `r4`, switches `s1`, `s2` e hosts `h1`, `h2`.
